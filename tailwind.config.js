/** @type {import('tailwindcss').Config} */
module.exports = {
  // for multiple or more than one file
  // content: ["./src/**/*{html, js, jsx, tsx}"],

  // for single file only
  content: ["./src/**/*.js"],
  theme: {
    extend: {},
  },
  plugins: [],
};
